# Aster & Row Support Agent - Environment Configuration Example
# No external API keys or secrets are required for local deterministic execution.

# Optional port for web UI server (defaults to 8000)
PORT=8000
